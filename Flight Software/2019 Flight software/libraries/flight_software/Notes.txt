See about wiping the camera buffer during setup if possible

For UDP:
https://www.arduino.cc/en/Tutorial/UDPSendReceiveString
No connection setup required

Multicasting:
https://tkkrlab.nl/wiki/Arduino_UDP_multicast



Sensor readings should be about every second.
Record the time of the last recording, and check it against millis() to see if >= 1000
Should telemetry transmit every second too?